// import type { MaybePromise } from "bun";
// import type { TSchema, Static } from "typebox";

// export interface TriggerHandler<Context, > {
// 	(
// 		ctx: Context,
// 		callback: (data: any) => MaybePromise<void>
// 	): MaybePromise<void>;
// }

// export interface Evaluator<Context> {
// 	context(): Context;
// 	triggers;
// 	conditions: Record<string, Condition<any>>;
// }
